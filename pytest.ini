[pytest]
testpaths = tests
addopts = -ra --tb=short
markers =
    slow: heavy tests that need the full ML stack (mlflow, torch, chromadb); run in the nightly workflow, excluded from the fast PR lane.
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
