# Core Spark + Delta stack
# NOTE: pyspark 3.5.x pairs with delta-spark 3.2.x (matched on the Delta compatibility matrix).
# 3.5.8 is the latest 3.5.x patch (patches CVE-2024-45855, encryption strength).
pyspark==3.5.8
delta-spark==3.2.1

# ML tracking + registry
# MLflow 3.11.1 patches the tracking-server and artifact-handler CVE clusters.
# Client APIs used here (log_param, log_metric, sklearn.log_model, register_model)
# are stable across the 2.x -> 3.x migration.
mlflow==3.14.0
scikit-learn==1.9.0
pandas==2.2.2
numpy==1.26.4

# RAG demo (all OSS, runs locally)
chromadb==0.5.5
sentence-transformers==3.0.1

# Serving demo
# 1.54.0 patches CVE-2025 SSRF on Windows and older path-traversal issue.
streamlit==1.59.2

# Data loading helper
# requests 2.34.2 patches netrc credential leak (CVE-2024-35195) and
# extract_zipped_paths tmpfile reuse.
requests==2.34.2
# pyarrow 23.0.1 patches the pre-buffering use-after-free (GHSA-8gxx-6f7v-vfv2).
pyarrow==23.0.1

# Dev tooling
# pytest 9.0.3 patches the tmpdir permissions issue (CVE-2024-8811 equivalent).
pytest==9.1.1
pytest-cov==7.1.0
ruff==0.15.22
